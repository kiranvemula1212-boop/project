spring.application.name=reporting-portal

# Only the health endpoint is exposed — this is an internal reporting API, not a
# platform that needs its full operational surface (env, beans, etc.) reachable over HTTP.
management.endpoints.web.exposure.include=health
